# Cadence live product-tour draft

Captured from `/Applications/Cadence.app` using macOS screen recording while navigating with native computer use. The source is a 24-second window recording, without microphone audio. Existing demo assets were preserved.

This is a visual draft, not a completed launch video. The live Compose settings reported “DeepSeek needs attention”, so successful generation and insertion were not staged or fabricated. The final hero sequence still needs a working Compose provider and a real dictation capture.

Files:
- `cadence-live-tour.mov`: original window recording.
- `cadence-launch-draft.mp4`: framed 1080p product-tour export.
- `render-frame.swift`: editable title-frame source.
- `launch-frame.png`: rendered frame.

Suggested final sequence: hold to dictate → lock recording → press Command to enter Compose → review the finished draft → insert into an unsent local document. Follow with a short model/settings shot and end card. Record only synthetic demo content.
